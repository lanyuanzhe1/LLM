WEBUI_SECRET_KEY=change-me
OPENAI_COMPAT_API_KEY=match-app-env
APP_UPSTREAM_BASE_URL=http://127.0.0.1:8000

# CORS：两种部署拓扑（vite dev 代理 / 后端静态托管 SPA）均为同源，默认不放行任何跨域来源。
# 仅在确需跨域访问时显式配置，多个来源用分号分隔：
# CORS_ALLOW_ORIGIN=http://localhost:5173;http://127.0.0.1:5173
